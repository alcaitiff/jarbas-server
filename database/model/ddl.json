[
  "CREATE TABLE IF NOT EXISTS intent (name TEXT PRIMARY KEY, data TEXT)",
  "CREATE TABLE IF NOT EXISTS persisted (property TEXT NOT NULL, value TEXT NOT NULL, UNIQUE(property))",
  "CREATE TABLE IF NOT EXISTS memory (owner TEXT, data TEXT NOT NULL)"
]